/**
 * lib/cart.ts — Helper functions for cart operations
 * 
 * getSessionId() ถูกย้ายไป lib/session.ts แล้ว
 * เพื่อคงความเข้ากันได้ ไฟล์นี้ยัง re-export ไว้
 */
export { getSessionId } from "./session";
